filesystem: "root"
mode: 0644
path: "/etc/kubernetes/static-pod-resources/coredns/Corefile.template"
contents:
  inline: |
    . {
        log
        mdns $DOMAIN 3 $NAME
        cache 30
        reload
        forward . /etc/resolv.conf {
            except $DOMAIN
        }
    }
