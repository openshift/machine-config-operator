mode: 0755
path: "/etc/kubernetes/static-pod-resources/keepalived/scripts/chk_ocp_script.sh.tmpl"
contents:
  inline: |
    #!/bin/bash
    [ -e /var/run/keepalived/haproxy-healthy ] && [ -e /var/run/keepalived/iptables-rule-exists ]
