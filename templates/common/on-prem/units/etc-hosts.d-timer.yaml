name: etc-hosts.d.timer
enabled: true
contents: |
  [Unit]
  Description=Run etc-hosts.d service periodically

  [Timer]
  OnBootSec=10sec
  OnUnitActiveSec=1min

  [Install]
  WantedBy=timers.target
