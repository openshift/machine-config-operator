name: etc-hosts.d.service
# This service is run by a timer
enabled: false
contents: |
  [Unit]
  Description=Append hosts file entries from /etc/hosts.d to /etc/hosts
  Before=kubelet.service crio.service

  [Service]
  Type=oneshot
  ExecStart=/bin/bash /usr/local/sbin/etc-hosts-appender.sh
