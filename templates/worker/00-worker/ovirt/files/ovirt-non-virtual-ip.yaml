filesystem: "root"
mode: 0755
path: "/usr/local/bin/non_virtual_ip"
contents:
  inline: |
    #!/usr/bin/bash
    podman run --rm \
      --authfile /var/lib/kubelet/config.json \
      --net=host \
      --entrypoint=/usr/bin/non-virtual-ip \
      {{ .Images.baremetalRuntimeCfgImage }} \
      ${@}
