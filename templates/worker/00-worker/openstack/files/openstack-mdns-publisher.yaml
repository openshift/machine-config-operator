filesystem: "root"
mode: 0644
path: "/etc/kubernetes/manifests/mdns-publisher.yaml"
contents:
  inline: |
    kind: Pod
    apiVersion: v1
    metadata:
      name: mdns-publisher
      namespace: kube-system
      creationTimestamp:
      deletionGracePeriodSeconds: 65
      labels:
        app: kni-infra-mdns
    spec:
      volumes:
      - name: resource-dir
        hostPath:
          path: "/etc/kubernetes/static-pod-resources/mdns"
      - name: conf-dir
        hostPath:
          path: "/etc/kubernetes/static-pod-resources/mdns"
      initContainers:
      - name: render-config
        image: quay.io/openshift/origin-node:latest
        command:
        - "/bin/bash"
        - "-c"
        - |
          #/bin/bash
          set -ex

          export CLUSTER_NAME={{(split "." .EtcdDiscoveryDomain)._0}}
          export DOMAIN={{.EtcdDiscoveryDomain}}

          #TODO(trown): find a better way to get the host's IP and HOSTNAME
          API_VIP="$(dig +noall +answer "api.${DOMAIN}" | awk '{print $NF}')"
          SUBNET_CIDR="$(ip addr show | grep -v "scope host" | grep -Po 'inet \K[\d.]+/[\d.]+' | head -n1)"
          PREFIX="${SUBNET_CIDR#*/}"
          export NON_VIRTUAL_IP="${SUBNET_CIDR%/*}"

          HOSTNAME_SUFFIX="$(hostname -I | awk '{print $1}' | tr . -)"
          export WORKER_HOSTNAME="host-$HOSTNAME_SUFFIX.local."

          /usr/libexec/platform-python -c "from __future__ import print_function
          import os
          with open('/etc/kubernetes/static-pod-resources/config.template', 'r') as f:
              content = f.read()
          with open('/etc/mdns/config.hcl', 'w') as dest:
              print(os.path.expandvars(content), file=dest)"
        resources: {}
        volumeMounts:
        - name: resource-dir
          mountPath: "/etc/kubernetes/static-pod-resources"
        - name: conf-dir
          mountPath: "/etc/mdns"
        imagePullPolicy: IfNotPresent
      containers:
      - name: mdns-publisher
        image: quay.io/openshift/origin-mdns-publisher:latest
        args:
        - "--debug"
        resources:
          requests:
            cpu: 150m
            memory: 1Gi
        volumeMounts:
        - name: conf-dir
          mountPath: "/etc/mdns"
        terminationMessagePolicy: FallbackToLogsOnError
        imagePullPolicy: IfNotPresent
      hostNetwork: true
      tolerations:
      - operator: Exists
      priorityClassName: system-node-critical
    status: {}
